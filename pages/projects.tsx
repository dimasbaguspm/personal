import React, { FC } from 'react';
import { BodyText } from '../components/atomic/typography';

interface ProjectProps {}

const Project: FC<ProjectProps> = () => {
  return <BodyText>Project</BodyText>;
};

export default Project;
